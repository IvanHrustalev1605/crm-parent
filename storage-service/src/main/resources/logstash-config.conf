input {
  jdbc {
    jdbc_driver_library => "C:\Users\Хрусталев Иван\.m2\repository\org\postgresql\postgresql\42.6.1\postgresql-42.6.1.jar"
    jdbc_driver_class => "org.postgresql.Driver"
    jdbc_connection_string => "jdbc:postgresql://127.0.0.1:22/storage_service"
    jdbc_user => "admin"
    jdbc_password => "postgres"
    statement => "SELECT * FROM storage.repair_parts"
    schedule => "*/10 * * * *"
  }
}
output {
  stdout {
    codec => rubydebug
  }
}
output {
    elasticsearch {
        ssl_certificate_verification => false
        hosts => ["localhost:9200"]
        index => "repair_parts"
        doc_as_upsert => true
        action => "update"
        document_id => "%{id}"
    }
}